{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "python -c \"from app import app; app.run(host='0.0.0.0', port=int(__import__('os').environ.get('PORT',8080)))\"",
    "restartPolicyType": "ALWAYS"
  }
}
